# Base URL of the CUNY Compass API. Copy this file to `.env` and adjust as needed.
# In dev this is your local API; use your ngrok URL when sharing across machines.
VITE_API_URL=http://localhost:3000
